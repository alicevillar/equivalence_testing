 Execute equivalence.py no espaço de trabalho Codio - Testando com Python - que é uma implementação de particionamento de equivalência. Este teste particiona inteiros [-3,5] em classes de equivalência com base em lambda x, y: (x-y)% 4 == 0.
Na saída, você deve ser capaz de ver como um conjunto de objetos a serem particionados é considerado e uma função avalia se os dois objetos são equivalentes antes de imprimir o resultado.
test_equivalence_partition () produz a seguinte saída:
set([1, -3]) set([2, -2]) set([3, -1]) set([0, 4]) 0 : set([0, 4]) 1 : set([1, -3]) 2 : set([2, -2]) 3 : set([3, -1]) 4 : set([0, 4]) -2 : set([2, -2]) -3 : set([1, -3]) -1 : set([3, -1])
# uma função lambda para fazer uma relação de teste:
lambda x, y:
	(x-y)% 4 == 0

Se for v, entao o set está formado. No dicionário vai ficar os sets, que formam uma partição.
Depois faz um exame de teste da partição para verificar se forma uma classe de equivalência.
A partição vai ficar em um dicionario, daí é possível verificar se existe equivalência, ouseja, se é reflexiva, simétrica e transitiva.

